<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/color_background"
    tools:context=".TimeSettingsActivity">

    <!-- 1. Toolbar -->
    <RelativeLayout
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize">

        <ImageView
            android:id="@+id/iv_back"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_arrow_back"
            app:tint="@color/white"
            android:padding="16dp"
            android:layout_centerVertical="true"
            android:background="?attr/selectableItemBackgroundBorderless"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/title_time_controls"
            android:textColor="@color/white"
            android:textSize="20sp"
            android:textStyle="bold"
            android:layout_centerInParent="true"/>

    </RelativeLayout>

    <!-- 2. "New Custom Time" Button -->
    <Button
        android:id="@+id/btn_new_custom_time"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/toolbar"
        android:layout_marginHorizontal="16dp"
        android:layout_marginTop="16dp"
        android:backgroundTint="@color/middle_bar"
        android:text="@string/button_new_custom_time"
        android:textColor="@color/white"
        app:icon="@drawable/ic_add"
        app:iconTint="@color/white"/>

    <!-- 3. "Presets" Label -->
    <TextView
        android:id="@+id/tv_presets_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/section_presets"
        android:textColor="@color/text_moves"
        android:textSize="16sp"
        android:layout_below="@id/btn_new_custom_time"
        android:layout_marginTop="24dp"
        android:layout_marginStart="16dp"/>

    <!-- 4. The List of Presets -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_view_presets"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/tv_presets_label"
        android:layout_above="@id/btn_start_game"
        android:layout_marginTop="8dp"/>

    <!-- 5. "Start" Button -->
    <Button
        android:id="@+id/btn_start_game"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:layout_alignParentBottom="true"
        android:layout_margin="16dp"
        android:backgroundTint="@color/color_accent_green"
        android:text="@string/button_start_game"
        android:textColor="@color/black"
        android:textSize="18sp"/>

</RelativeLayout>
